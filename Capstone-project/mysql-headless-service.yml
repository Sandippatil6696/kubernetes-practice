kind: Service
apiVersion: v1
metadata:
  name: headless-service
  namespace: capstone
  labels:
    app: mysql-db
spec:
  clusterIP: None
  selector:
    app: mysql-db
  ports:
    - port: 3306
      targetPort: 3306
